
         var canvas = document.getElementById("lienzo");
        if (canvas && canvas.getContext) {
        var ctx = canvas.getContext("2d");
            if (ctx) {var R = 30;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx.fillStyle = "#f1f1f1";
                      ctx.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx.lineTo(x, y);
                      }
                      ctx.closePath();
                      ctx.fill();
                      ctx.stroke();
            }

                ctx.font = "bold 15px sans-serif";
                ctx.fillStyle = 'black';
                ctx.fillText("1",130,20);
                ctx.fillText("2",270,50);
                ctx.fillText("3",335,180);
                ctx.fillText("4",280,310);
                ctx.fillText("5",140,340);
                ctx.fillText("6",20,260);
                ctx.fillText("7",20,115);


        }

        var canvas = document.getElementById("lienzo2");
        if (canvas && canvas.getContext) {
        var ctx2 = canvas.getContext("2d");
            if (ctx2) {var R = 45;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx2.fillStyle = "#f1f1f1";
                      ctx2.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx2.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx2.lineTo(x, y);
                      }
                      ctx2.closePath();
                      ctx2.fill();
                      ctx2.stroke();
            }


        }

        var canvas = document.getElementById("lienzo3");
        if (canvas && canvas.getContext) {
        var ctx3 = canvas.getContext("2d");
            if (ctx3) {var R = 60;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx3.fillStyle = "#f1f1f1";
                      ctx3.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx3.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx3.lineTo(x, y);
                      }
                      ctx3.closePath();
                      ctx3.fill();
                      ctx3.stroke();
            }


        }
        var canvas = document.getElementById("lienzo4");
        if (canvas && canvas.getContext) {
        var ctx4 = canvas.getContext("2d");
            if (ctx4) {var R = 75;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx4.fillStyle = "#f1f1f1";
                      ctx4.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx4.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx4.lineTo(x, y);
                      }
                      ctx4.closePath();
                      ctx4.fill();
                      ctx4.stroke();
            }


        }
        var canvas = document.getElementById("lienzo5");
        if (canvas && canvas.getContext) {
        var ctx5 = canvas.getContext("2d");
            if (ctx5) {var R = 90;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx5.fillStyle = "#f1f1f1";
                      ctx5.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx5.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx5.lineTo(x, y);
                      }
                      ctx5.closePath();
                      ctx5.fill();
                      ctx5.stroke();
            }


        }

        var canvas = document.getElementById("lienzo6");
        if (canvas && canvas.getContext) {
        var ctx6 = canvas.getContext("2d");
            if (ctx6) {var R = 105;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx6.fillStyle = "#f1f1f1";
                      ctx6.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx6.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx6.lineTo(x, y);
                      }
                      ctx6.closePath();
                      ctx6.fill();
                      ctx6.stroke();
            }


        }
        var canvas = document.getElementById("lienzo7");
        if (canvas && canvas.getContext) {
        var ctx7 = canvas.getContext("2d");
            if (ctx7) {var R = 120;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx7.fillStyle = "#f1f1f1";
                      ctx7.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx7.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx7.lineTo(x, y);
                      }
                      ctx7.closePath();
                      ctx7.fill();
                      ctx7.stroke();
            }


        }
        var canvas = document.getElementById("lienzo16");
        if (canvas && canvas.getContext) {
        var ctx7 = canvas.getContext("2d");
            if (ctx7) {var R = 135;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx7.fillStyle = "#f1f1f1";
                      ctx7.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx7.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx7.lineTo(x, y);
                      }
                      ctx7.closePath();
                      ctx7.fill();
                      ctx7.stroke();
            }


        }
        var canvas = document.getElementById("lienzo17");
        if (canvas && canvas.getContext) {
        var ctx7 = canvas.getContext("2d");
            if (ctx7) {var R = 150;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx7.fillStyle = "#f1f1f1";
                      ctx7.strokeStyle = '#000000';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx7.beginPath();
                      for( var i = 0; i<7; i++ ){
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx7.lineTo(x, y);
                      }
                      ctx7.closePath();
                      ctx7.fill();
                      ctx7.stroke();
            }


        }

        var canvas = document.getElementById("lienzo8");
        if (canvas && canvas.getContext) {
        var ctx8 = canvas.getContext("2d");
            if (ctx8) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx8.fillStyle = "#f1f1f1";
                      ctx8.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx8.beginPath();
                      i=0
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx8.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx8.lineTo(x, y);
                      ctx8.closePath();
                      ctx8.fill();
                      ctx8.stroke();
            }


        }
        var canvas = document.getElementById("lienzo9");
        if (canvas && canvas.getContext) {
        var ctx9 = canvas.getContext("2d");
            if (ctx9) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx9.fillStyle = "#f1f1f1";
                      ctx9.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx9.beginPath();
                      i=1
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx9.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx9.lineTo(x, y);
                      ctx9.closePath();
                      ctx9.fill();
                      ctx9.stroke();
            }


        }

        var canvas = document.getElementById("lienzo10");
        if (canvas && canvas.getContext) {
        var ctx10 = canvas.getContext("2d");
            if (ctx10) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx10.fillStyle = "#f1f1f1";
                      ctx10.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx10.beginPath();
                      i=2
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx10.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx10.lineTo(x, y);
                      ctx10.closePath();
                      ctx10.fill();
                      ctx10.stroke();
            }


        }
        var canvas = document.getElementById("lienzo11");
        if (canvas && canvas.getContext) {
        var ctx11 = canvas.getContext("2d");
            if (ctx11) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx11.fillStyle = "#f1f1f1";
                      ctx11.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx11.beginPath();
                      i=3
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx11.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx11.lineTo(x, y);
                      ctx11.closePath();
                      ctx11.fill();
                      ctx11.stroke();
            }


        }
        var canvas = document.getElementById("lienzo12");
        if (canvas && canvas.getContext) {
        var ctx12 = canvas.getContext("2d");
            if (ctx12) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx12.fillStyle = "#f1f1f1";
                      ctx12.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx12.beginPath();
                      i=4
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx12.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx12.lineTo(x, y);
                      ctx12.closePath();
                      ctx12.fill();
                      ctx12.stroke();
            }


        }
        var canvas = document.getElementById("lienzo13");
        if (canvas && canvas.getContext) {
        var ctx13 = canvas.getContext("2d");
            if (ctx13) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx13.fillStyle = "#f1f1f1";
                      ctx13.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx13.beginPath();
                      i=5
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx13.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx13.lineTo(x, y);
                      ctx13.closePath();
                      ctx13.fill();
                      ctx13.stroke();
            }


        }
        var canvas = document.getElementById("lienzo14");
        if (canvas && canvas.getContext) {
        var ctx14 = canvas.getContext("2d");
            if (ctx14) {var R = 0;
                      var X = canvas.width/2;
                      var Y = canvas.height/2;
                      ctx14.fillStyle = "#f1f1f1";
                      ctx14.strokeStyle = '#909090';
                      // un angulo de 60deg.
                      var rad = ( Math.PI / 180 ) * 51;
                      ctx14.beginPath();
                      i=6
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx14.lineTo(x, y);
                      R=150
                      x = X + R * Math.cos( rad*i );
                      y = Y + R * Math.sin( rad*i );
                      ctx14.lineTo(x, y);
                      ctx14.closePath();
                      ctx14.fill();
                      ctx14.stroke();
            }


        }


        $( "#enviar" ).click(function() {
            suma1 = switch1 + switch2 + switch3 + switch4 + switch5 + switch6 + switch7 + switch8 + switch9;
            suma2 = switch10 + switch11 + switch12 + switch13 + switch14 + switch15 + switch16 + switch17 + switch18;
            suma3 = switch19 + switch20 + switch21 + switch22 + switch23 + switch24 + switch25 + switch26 + switch27;
            suma4 = switch28 + switch29 + switch30 + switch31 + switch32 + switch33 + switch34 + switch35 + switch36;
            suma5 = switch37 + switch38 + switch39 + switch40 + switch41 + switch42 + switch43 + switch44 + switch45;
            suma6 = switch46 + switch47 + switch48 + switch49 + switch50 + switch51 + switch52 + switch53 + switch54;
            suma7 = switch55 + switch56 + switch57 + switch58 + switch59 + switch60 + switch61 + switch62 + switch63;
            $( "#lienzo15" ).replaceWith( "<canvas id='lienzo15' height='350' width='350' class='lienzo'>Su navegador no soporta canvas :( </canvas>");
            var canvas = document.getElementById("lienzo15");
            if (canvas && canvas.getContext) {
            var ctx15 = canvas.getContext("2d");
                if (ctx15) {

                          var X = canvas.width/2;
                          var Y = canvas.height/2;
                          ctx15.fillStyle = "rgba(255,76,76,0.6)";
                          ctx15.strokeStyle = '#ff4c4c';
                          // un angulo de 60deg.
                          var rad = ( Math.PI / 180 ) * 51;
                          ctx15.beginPath();
                          var plus = 0;
                          if (suma1 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }

                          var R = suma1 * 15 + plus;
                          i=0;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          if (suma2 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }
                          R = suma2 * 15 + plus;
                          i = 1;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          if (suma3 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }
                          R = suma3 * 15 + plus;
                          i = 2;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          if (suma4 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }
                          R = suma4 * 15 + plus;
                          i = 3;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          if (suma5 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }
                          R = suma5 * 15 + plus;
                          i = 4;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          if (suma6 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }
                          R = suma6 * 15 + plus;
                          i = 5;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          if (suma7 != 0){
                            plus = 15;
                          } 
                          else{
                            plus = 0;
                          }
                          R = suma7 * 15 + plus;
                          i = 6;
                          x = X + R * Math.cos( rad*i );
                          y = Y + R * Math.sin( rad*i );
                          ctx15.lineTo(x, y);
                          ctx15.closePath();
                          ctx15.fill();
                          ctx15.stroke();
                }


            }


            $( "#centroradar" ).show(0);
        });
